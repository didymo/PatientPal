/**
 * This class will store the data of the tab views
 */
export interface Tabview {
    label: string;
    entityId: number;
    // obdId: number;
    // parentView: number;
    // koboFormId: string;
}
